meta {
  name: col2_DELETEGoRest
  type: http
  seq: 9
}

delete {
  url: {{gorest_base_url}}{{gorest_users_path}}/{{gorest_user_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{gorest_token}}
}

tests {
  test("Status 204 No Content", () => expect(res.status).to.equal(204));
}

settings {
  encodeUrl: true
  timeout: 0
}
